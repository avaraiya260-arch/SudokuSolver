# SudokuSolver
A Python sudoku solver that uses backtracking to find the solution. It scans for empty cells, tries numbers 1–9, checks each against the row, column, and 3×3 box, and backtracks when stuck. Edit the board variable at the top to use your own puzzle. No dependencies.
